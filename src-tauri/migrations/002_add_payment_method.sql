-- Payment method is stored inside the JSON blob for appointments.
-- This migration is a placeholder — no schema change needed here
-- since all fields live in records.data (JSON).
-- Future migrations go in 003_*.sql, 004_*.sql, etc.

SELECT 1; -- no-op
